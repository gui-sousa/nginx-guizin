apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer
  selector:
    app: nginx-project
  ports:
    - name: http-port
      port: 80
      targetPort: 80
