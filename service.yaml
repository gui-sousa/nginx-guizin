apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: default
spec:
  type: NodePort
  ports:
    - name: http-port
      port: 8082
      targetPort: 80
  selector:
    app: nginx-project